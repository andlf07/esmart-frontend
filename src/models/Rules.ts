export interface Rules {
  id: string;
  fact: string;
  operator: string;
  value: string;
  description: string;
  checkKey: string;
  enabled: boolean;
  createdAt: string;
  updatedAt: string;
  sensor_id: string;
}
